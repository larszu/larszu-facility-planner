{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src"],
  // Tests, die das DATEISYSTEM lesen, tragen `.node.test.ts` und gehoeren
  // nicht in dieses Projekt: hier ist `lib` auf DOM gestellt und `types`
  // kennt kein Node. Sie stehen stattdessen in `tsconfig.node.json`, das
  // beides hat — dieselbe Trennung, die `vite.config.ts` dort schon hat.
  //
  // Der Grund fuer die Namensregel statt einer Dateiliste: eine Liste
  // vergisst der Naechste, ein Suffix nicht. Uebernommen aus dem
  // `inventory-planner`, wo derselbe Schnitt schon steht.
  "exclude": ["src/**/*.node.test.ts"]
}
